use tools::isEven as is_even;

type str_array = string[];

enum Dico {
    first,
    second
}

struct point {
    x: float,
    y: float
}

fun main() -> int
{
    let f_val: float = .2;
    let f_nbr: float = 5.;
    let c: char = 'c';
    let nbr: int = 5;
    let special_c: char = '\n';

    const x: int, y: int;
    
    x = 0;
    y = 1;

    if x < y {
        print_ln(s);
    } else if x == y {
        print_ln(x);
    } else {
        print_ln(y);
    }

    for c in s {
        print(c);
    }
    print_ln();

    for i in 0..5 {
        print_ln(i);
        continue;
    }

    loop {
        break;
    }

    while x < i {
        print_ln("Hello");
    }

    if is_even(nbr) {
        print_ln("no");
    }
    if is_even(nbr - 1) {
        print_ln("yes");
    }

    const c: string = "yes";

    return 0;
}
