fun main() -> int
{
    let s: string = "Hello, World!\n";
    let f: float  = .2;
    let c: char   = 'c';
    let i: int    = 5;
    let cs: char  = '\n';

    const x: int, y: int;
    
    x = 0;
    y = 1;

    if x < y {
        print_ln(s);
    } else if x == y {
        print_ln(x);
    } else {
        print_ln(y);
    }

    for c in s {
        print(c);
    }
    print_ln();

    for i in 0..5 {
        print_ln(i);
        continue;
    }

    loop {
        break;
    }

    while x < i {
        print_ln("Hello");
    }

    return 0;
}
